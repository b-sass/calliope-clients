import { BookCard } from "../ui/BookCard";
import styles from "./home.module.css";

const Home = () => {
  return (
    <div className={styles.home}>
      {/* <BookCard title={"A very long title"} progress={"67%"} /> */}
    </div>
  );
};

export { Home };
